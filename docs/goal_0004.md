# Goal 0004: Improve Zig `insert_reserved` Performance

Optimize the Zig `zig_phmap` reserved-insert hot path until it is at least on-par with C++ `parallel-hashmap` on the same benchmark workload, with particular attention to x86_64 GitHub Actions performance.

## Background

Goal 0003 made the Zig port competitive or faster than C++ on most hash-map workloads, including lookup hits, iteration, remove, and string-key operations. The remaining concern from the GitHub Actions x86_64 benchmark is `insert_reserved`.

Recent GitHub Actions run `25212031553` on `ubuntu-24.04` x86_64, commit `f6262674d44d6fde241c60888e68c575993f5ab1`, reported:

- Zig `insert_reserved`: 40.122 ns/op
- C++ `insert_reserved`: 30.609 ns/op
- Gap: Zig is 31.1% slower

Local aarch64 median results from the Goal 0003 final audit reported:

- Zig `insert_reserved`: 25.472 ns/op
- C++ `insert_reserved`: 25.779 ns/op
- Gap: Zig is 1.2% faster

This means the reserved-insert path is effectively solved on local aarch64, but still underperforms on hosted x86_64. The next goal is to understand and remove that architecture-sensitive gap without regressing the workloads that Goal 0003 fixed.

## Objective

Improve Zig `insert_reserved` performance so it is at least on-par with C++ `parallel-hashmap` on both:

- local development hardware
- GitHub Actions `ubuntu-24.04` x86_64

Primary target:

- `insert_reserved`: Zig must be no slower than C++ on the same machine and benchmark run.

Secondary targets:

- Do not regress `lookup_hit`, `lookup_miss`, `iterate`, `mixed`, `remove`, `string_insert`, `string_lookup`, `high_load_miss`, or `tombstone_churn` by more than 10% from the Goal 0003 final audit unless the regression is documented and justified.
- Preserve all public APIs.
- Preserve allocation-failure correctness.
- Preserve node-container pointer stability.
- Preserve the benchmark comparison between Zig and C++ in GitHub Actions.

## Baseline To Beat

Use the existing Zig and C++ benchmark workloads unless this goal deliberately fixes benchmark fairness. If benchmark code changes, record old and new numbers.

GitHub Actions x86_64 baseline from run `25212031553`:

- Zig `insert_reserved`: 40.122 ns/op
- C++ `insert_reserved`: 30.609 ns/op
- Zig gap: 31.1% slower

Local aarch64 baseline from Goal 0003 final audit:

- Zig `insert_reserved`: 25.472 ns/op
- C++ `insert_reserved`: 25.779 ns/op
- Zig gap: 1.2% faster

Required final threshold:

- Zig `insert_reserved` <= C++ `insert_reserved` on local benchmark runs.
- Zig `insert_reserved` <= C++ `insert_reserved` on GitHub Actions x86_64 benchmark runs.

Use multiple benchmark runs locally and compare medians or a clearly documented stable sample. Do not claim parity from a single noisy outlier.

## Required Investigation

Before changing code, inspect the current Zig reserved-insert path and the upstream C++ equivalent.

Inspect the Zig implementation for:

- `put`, `insert`, `getOrPut`, and the internal insertion helper used by `insert_reserved`
- hash splitting into H1/H2
- probe-sequence initialization and advancement
- first non-full-slot search
- equality checks during insertion
- growth-left accounting
- tombstone reuse
- control-byte writes and cloned-control-byte maintenance
- slot initialization and value movement
- any extra safety checks or branches still present in ReleaseFast

Inspect upstream C++ from:

- `.deps/parallel-hashmap-2.0.0/parallel_hashmap/phmap.h`

Focus on:

- `raw_hash_set::emplace`
- `find_or_prepare_insert`
- `prepare_insert`
- `find_first_non_full`
- control-byte writes
- growth-left accounting
- resize/tombstone cleanup policy

Record source-inspection findings in `checkpoints.md` before making performance changes.

## Optimization Direction

Focus on production hot-path improvements, not benchmark-only shortcuts.

Likely areas to evaluate:

- Avoid duplicated probe work between lookup and insertion.
- Avoid unnecessary equality checks when the first probed group has an empty slot.
- Reduce branches in the reserved-insert fast path.
- Separate the no-grow reserved-insert path from the general grow-capable insert path if it measurably helps.
- Keep `growth_left` and tombstone accounting branch-light.
- Ensure H2/control-byte writes and cloned-control-byte updates are minimal and correctly ordered.
- Check whether slot initialization performs avoidable copies or default initialization.
- Evaluate whether Zig vector/scalar codegen differs materially on x86_64 versus aarch64.
- Use targeted inline hints only when measurements show a real improvement.

Do not reduce correctness checks, remove tests, or specialize behavior only for benchmark key types.

## Correctness Requirements

All existing correctness behavior must remain intact.

Correctness gates:

```sh
/home/wr/gh/zig_tree/.toolchains/zig-aarch64-linux-0.17.0-dev.135+9df02121d/zig build test
/home/wr/gh/zig_tree/.toolchains/zig-aarch64-linux-0.17.0-dev.135+9df02121d/zig build -Doptimize=ReleaseSafe test
/home/wr/gh/zig_tree/.toolchains/zig-aarch64-linux-0.17.0-dev.135+9df02121d/zig build -Doptimize=ReleaseFast test
```

Use this exact Zig toolchain unless a newer goal explicitly changes it.

Add or update tests if changes affect:

- tombstone reuse
- insertion through long probe chains
- insertion near cloned control-byte boundaries
- duplicate insert behavior
- growth-left accounting
- reserve followed by dense insertion
- allocation failure during growth or rehash

## Benchmark Requirements

Benchmark both Zig and C++ after every meaningful optimization.

Required local commands:

```sh
g++ -O3 -DNDEBUG -std=c++17 -I .deps/parallel-hashmap-2.0.0 .deps/parallel_hashmap_bench.cc -o .deps/parallel_hashmap_bench
/home/wr/gh/zig_tree/.toolchains/zig-aarch64-linux-0.17.0-dev.135+9df02121d/zig build -Doptimize=ReleaseFast bench
.deps/parallel_hashmap_bench
```

Required CI validation:

- Keep a GitHub Actions benchmark that runs both Zig and C++ `parallel-hashmap`.
- The CI output must include `insert_reserved` for both implementations.
- The CI output must make the Zig-vs-C++ gap visible enough to audit from the run logs.
- Final validation must cite the GitHub Actions run ID and commit SHA.

Required workloads to watch for regressions:

- `insert_reserved`
- `lookup_hit`
- `lookup_miss`
- `iterate`
- `mixed`
- `remove`
- `string_insert`
- `string_lookup`
- `high_load_miss`
- `tombstone_churn`

## Checkpoint Requirements

Update `checkpoints.md` after every meaningful step.

Each checkpoint must include:

- timestamp
- short description
- files changed
- source-inspection notes when relevant
- benchmark commands
- Zig benchmark results
- C++ benchmark results
- percentage gaps for all measured operations
- correctness commands and pass/fail status
- notes on regressions
- next optimization hypothesis

Required checkpoints:

- source inspection of Zig and C++ reserved-insert paths
- pre-change local benchmark baseline
- first insert hot-path optimization
- first correctness milestone after optimization
- first benchmark showing local `insert_reserved` parity
- first GitHub Actions benchmark after the change
- final audit comparing local aarch64 and CI x86_64 results

## Completion Criteria

Produce a final `checkpoints.md` entry summarizing:

- starting local aarch64 `insert_reserved` performance
- ending local aarch64 `insert_reserved` performance
- starting GitHub Actions x86_64 `insert_reserved` performance
- ending GitHub Actions x86_64 `insert_reserved` performance
- C++ comparison and percentage gap for each measured workload
- correctness verification
- allocation-failure verification
- remaining known bottlenecks

Stop only when either:

- Zig `insert_reserved` is on-par with or faster than C++ locally and on GitHub Actions x86_64, with correctness gates green, or
- further improvement requires a larger architecture change, and `checkpoints.md` clearly explains the blocker and the proposed next design.
